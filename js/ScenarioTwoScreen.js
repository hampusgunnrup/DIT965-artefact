'use strict';

class ScenarioTwoScreen extends GameScreen {
    constructor(game) {
        super(game);
        
        this.background = "#000";
        
    }
    
    update(deltaTime) {
        super.update(deltaTime);
    }
}